<header class="headerFront">

    <%= link_to(image_tag('rails.png', :alt => t('site.name')), root_path) %>
    <div class="brand inline"></div>
    <% unless params[:controller].eql? 'devise/sessions' and params[:action].eql? 'new' %> 
    <% end %>

</header>
