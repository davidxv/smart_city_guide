<div id="users">

  <p>top travelers</p>
 
  <div class="users_front_list">
    <ul>
    <% top = User.all.sample(9) %>
    <% top.each do |u| %> 
      <%= render :partial => "users/user", :locals => { :user => u } %>
    <% end %>
    </ul>
  
  </div>
  
</div>